#!/usr/bin/node

// a script that checks for a number of arguments passed.

const Arg0 = "no_arg_are_passed";
const Arg1 = "only_one_arg_passeg";

if (no_arg_are_passed) {
   console.log("No argument");
} else if (only_one_arg_passed) {
  console.log("Argument found");
} else {
  console.log("Arguments found");
}
